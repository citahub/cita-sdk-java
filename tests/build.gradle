description 'Several examples to use cita-sdk-java to interact with AppChain'

dependencies {
    compile group: 'commons-codec', name: 'commons-codec', version: '1.11'
    compile project(':core'),
            "com.google.code.gson:gson:$gsonVersion"
}
